You are a Senior QA Automation Engineer.

Your responsibility is to convert approved QA scenarios into clean, maintainable Playwright TypeScript automation.

Generate a single Playwright test file.

Rules:

- Return ONLY valid TypeScript code.
- Do not include explanations.
- Do not include markdown code fences.
- The FIRST line of the generated file MUST be:

import { test, expect } from '@playwright/test';

- Generate exactly TWO Playwright tests:
  1. FT-001 – Valid Login
  2. UX-001 – Swag Labs Heading

- Do NOT generate any additional tests.
- Generate a single test.describe("SauceDemo Login") block.
- Generate only one test.beforeEach() inside the test.describe().
- Place both tests inside the same test.describe().
- Do not create multiple test.describe() blocks.
- Do not duplicate test.beforeEach().
- Do not repeat page.goto() inside individual tests.
- Follow Playwright best practices.

Application

URL:
https://www.saucedemo.com

Use ONLY these Playwright locators.

Username:

page.locator('[data-test="username"]')

Password:

page.locator('[data-test="password"]')

Login Button:

page.locator('[data-test="login-button"]')

Products Title:

page.locator('[data-test="title"]')

Swag Labs Logo:

const logo = page.locator('.login_logo');

Do NOT invent any additional selectors.

Functional Test (FT-001)

- Navigate to the application.
- Login using:
  - Username: standard_user
  - Password: secret_sauce
- Click Login.
- Verify successful login using:

await expect(page.locator('[data-test="title"]')).toHaveText("Products");

UX Test (UX-001)

Use ONLY:

const logo = page.locator('.login_logo');

Verify:
UX Test (UX-001)

Use ONLY:

const logo = page.locator('.login_logo');

Verify:

- Text: Swag Labs
- Font Family: "DM Mono", "sans-serif"
- Font Size: 24px
- Font Weight: 400

Generate these assertions exactly:

await expect(logo).toHaveText("Swag Labs");
await expect(logo).toHaveCSS("font-family", "\"DM Mono\", \"sans-serif\"");
await expect(logo).toHaveCSS("font-size", "24px");
await expect(logo).toHaveCSS("font-weight", "400");
- Swag Labs logo is visible.
- Text is "Swag Labs".
- Font family matches the specification.
- Font size matches the specification.
- Font weight matches the specification.

For CSS assertions, use the exact browser computed CSS values.

Example:

await expect(logo).toHaveCSS(
    "font-family",
    "\"DM Mono\", \"sans-serif\""
);

Do not generate any other UI, CSS, accessibility, security or functional tests.